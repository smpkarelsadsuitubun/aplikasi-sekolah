<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <title>SMP Karel Sadsuitubun - Manajemen PD</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --blue-deep: #0A2E5C;
            --blue-primary: #0D47A1;
            --blue-medium: #1565C0;
            --blue-light: #1976D2;
            --blue-soft: #42A5F5;
            --blue-pale: #E3F2FD;
            --blue-bg: #F0F5FF;
            --white: #FFFFFF;
            --gray-50: #F8FAFC;
            --gray-100: #F1F5F9;
            --gray-200: #E2E8F0;
            --gray-300: #CBD5E1;
            --gray-600: #475569;
            --gray-800: #1E293B;
            --red: #DC2626;
            --green: #16A34A;
            --amber: #F59E0B;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Plus Jakarta Sans', sans-serif; background: var(--blue-bg); color: var(--gray-800); overflow-x: hidden; }

        /* ====== LOGIN PAGE ====== */
        .login-page {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--blue-deep) 0%, var(--blue-primary) 40%, var(--blue-medium) 70%, var(--blue-light) 100%);
            position: relative;
            overflow: hidden;
        }
        .login-page::before {
            content: '';
            position: absolute;
            width: 600px; height: 600px;
            background: radial-gradient(circle, rgba(66,165,245,0.15) 0%, transparent 70%);
            top: -200px; right: -100px;
            border-radius: 50%;
        }
        .login-page::after {
            content: '';
            position: absolute;
            width: 400px; height: 400px;
            background: radial-gradient(circle, rgba(66,165,245,0.1) 0%, transparent 70%);
            bottom: -100px; left: -50px;
            border-radius: 50%;
        }
        .login-card {
            background: var(--white);
            border-radius: 20px;
            padding: 40px 36px;
            width: 90%;
            max-width: 420px;
            box-shadow: 0 25px 60px rgba(0,0,0,0.3);
            position: relative;
            z-index: 1;
            animation: slideUp 0.6s ease-out;
        }
        @keyframes slideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .login-logo { width: 80px; height: 80px; border-radius: 16px; object-fit: contain; margin: 0 auto 16px; display: block; }
        .login-card h1 { text-align: center; font-size: 1.25rem; font-weight: 800; color: var(--blue-deep); margin-bottom: 4px; }
        .login-card p.subtitle { text-align: center; font-size: 0.8rem; color: var(--gray-600); margin-bottom: 28px; }
        .form-group { margin-bottom: 18px; }
        .form-group label { display: block; font-size: 0.8rem; font-weight: 600; color: var(--gray-600); margin-bottom: 6px; }
        .form-group input {
            width: 100%; padding: 12px 14px 12px 42px;
            border: 2px solid var(--gray-200); border-radius: 12px;
            font-size: 0.95rem; font-family: inherit;
            transition: border-color 0.2s, box-shadow 0.2s;
            background: var(--gray-50);
        }
        .form-group input:focus { outline: none; border-color: var(--blue-light); box-shadow: 0 0 0 3px rgba(25,118,210,0.15); background: var(--white); }
        .input-wrapper { position: relative; }
        .input-wrapper i { position: absolute; left: 14px; top: 50%; transform: translateY(-50%); color: var(--gray-300); font-size: 0.9rem; }
        .btn-login {
            width: 100%; padding: 13px;
            background: linear-gradient(135deg, var(--blue-primary), var(--blue-light));
            color: var(--white); border: none; border-radius: 12px;
            font-size: 1rem; font-weight: 700; font-family: inherit;
            cursor: pointer; transition: transform 0.15s, box-shadow 0.2s;
            margin-top: 8px;
        }
        .btn-login:hover { transform: translateY(-1px); box-shadow: 0 8px 20px rgba(13,71,161,0.35); }
        .btn-login:active { transform: translateY(0); }
        .login-error { color: var(--red); font-size: 0.8rem; text-align: center; min-height: 20px; margin-top: 8px; font-weight: 500; }

        /* ====== SIDEBAR ====== */
        .app-container { display: none; min-height: 100vh; }
        .app-container.active { display: flex; }
        .sidebar {
            width: 260px; min-height: 100vh;
            background: linear-gradient(180deg, var(--blue-deep) 0%, var(--blue-primary) 100%);
            color: var(--white);
            display: flex; flex-direction: column;
            position: fixed; left: 0; top: 0; bottom: 0;
            z-index: 100;
            transition: transform 0.3s ease;
        }
        .sidebar-header {
            padding: 24px 20px 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            text-align: center;
        }
        .sidebar-logo { width: 60px; height: 60px; border-radius: 12px; object-fit: contain; margin: 0 auto 10px; display: block; background: var(--white); padding: 4px; }
        .sidebar-header h2 { font-size: 0.85rem; font-weight: 700; line-height: 1.3; }
        .sidebar-nav { flex: 1; padding: 16px 12px; }
        .nav-item {
            display: flex; align-items: center; gap: 12px;
            padding: 13px 16px; border-radius: 12px;
            color: rgba(255,255,255,0.7); cursor: pointer;
            transition: all 0.2s; font-size: 0.9rem; font-weight: 500;
            margin-bottom: 4px; text-decoration: none;
        }
        .nav-item:hover { background: rgba(255,255,255,0.1); color: var(--white); }
        .nav-item.active { background: rgba(255,255,255,0.18); color: var(--white); font-weight: 700; box-shadow: 0 2px 8px rgba(0,0,0,0.15); }
        .nav-item i { width: 20px; text-align: center; font-size: 1rem; }
        .sidebar-footer { padding: 16px 12px; border-top: 1px solid rgba(255,255,255,0.1); }
        .nav-logout { color: rgba(255,255,255,0.5); }
        .nav-logout:hover { background: rgba(220,38,38,0.2); color: #FCA5A5; }

        /* ====== MAIN CONTENT ====== */
        .main-content {
            flex: 1; margin-left: 260px;
            min-height: 100vh; display: flex; flex-direction: column;
        }
        .top-bar {
            background: var(--white);
            padding: 16px 24px;
            display: flex; align-items: center; justify-content: space-between;
            box-shadow: 0 1px 4px rgba(0,0,0,0.06);
            position: sticky; top: 0; z-index: 50;
        }
        .top-bar-left { display: flex; align-items: center; gap: 14px; }
        .hamburger { display: none; background: none; border: none; font-size: 1.3rem; color: var(--blue-primary); cursor: pointer; padding: 4px; }
        .top-bar h1 { font-size: 1.15rem; font-weight: 800; color: var(--blue-deep); }
        .top-bar .user-badge {
            font-size: 0.75rem; font-weight: 600;
            background: var(--blue-pale); color: var(--blue-primary);
            padding: 6px 14px; border-radius: 20px;
        }
        .page-content { flex: 1; padding: 24px; }
        .page-section { display: none; animation: fadeIn 0.3s ease; }
        .page-section.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }

        /* ====== DASHBOARD ====== */
        .stat-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 24px; }
        .stat-card {
            background: var(--white);
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            display: flex; align-items: center; justify-content: space-between;
            border-left: 5px solid var(--blue-light);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .stat-card:hover { transform: translateY(-2px); box-shadow: 0 6px 20px rgba(0,0,0,0.1); }
        .stat-card .stat-info h3 { font-size: 0.75rem; font-weight: 600; color: var(--gray-600); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px; }
        .stat-card .stat-info .stat-number { font-size: 2rem; font-weight: 800; color: var(--blue-deep); }
        .stat-card .stat-logo { width: 64px; height: 64px; border-radius: 12px; object-fit: contain; background: var(--blue-pale); padding: 6px; }
        .announcement-card {
            background: linear-gradient(135deg, var(--blue-primary), var(--blue-light));
            border-radius: 16px;
            padding: 24px 28px;
            color: var(--white);
            box-shadow: 0 4px 16px rgba(13,71,161,0.25);
        }
        .announcement-card h3 { font-size: 1rem; font-weight: 700; margin-bottom: 10px; display: flex; align-items: center; gap: 8px; }
        .announcement-card p { font-size: 0.88rem; line-height: 1.7; opacity: 0.92; }

        /* ====== TABLE ====== */
        .toolbar {
            display: flex; flex-wrap: wrap; gap: 10px;
            margin-bottom: 16px; align-items: center;
        }
        .btn {
            display: inline-flex; align-items: center; gap: 8px;
            padding: 10px 18px; border-radius: 10px;
            font-size: 0.82rem; font-weight: 600; font-family: inherit;
            cursor: pointer; border: none; transition: all 0.2s;
            white-space: nowrap;
        }
        .btn-blue { background: var(--blue-primary); color: var(--white); }
        .btn-blue:hover { background: var(--blue-medium); box-shadow: 0 4px 12px rgba(13,71,161,0.3); }
        .btn-green { background: var(--green); color: var(--white); }
        .btn-green:hover { background: #15803D; }
        .btn-amber { background: var(--amber); color: var(--white); }
        .btn-amber:hover { background: #D97706; }
        .btn-red { background: var(--red); color: var(--white); }
        .btn-red:hover { background: #B91C1C; }
        .btn-outline { background: transparent; color: var(--blue-primary); border: 2px solid var(--blue-light); }
        .btn-outline:hover { background: var(--blue-pale); }
        .search-box {
            flex: 1; min-width: 200px; position: relative;
        }
        .search-box input {
            width: 100%; padding: 10px 14px 10px 40px;
            border: 2px solid var(--gray-200); border-radius: 10px;
            font-size: 0.88rem; font-family: inherit;
            transition: border-color 0.2s; background: var(--white);
        }
        .search-box input:focus { outline: none; border-color: var(--blue-light); box-shadow: 0 0 0 3px rgba(25,118,210,0.1); }
        .search-box i { position: absolute; left: 14px; top: 50%; transform: translateY(-50%); color: var(--gray-300); }
        .table-wrapper {
            background: var(--white);
            border-radius: 16px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
            overflow: hidden;
            margin-bottom: 16px;
        }
        .table-scroll { overflow-x: auto; }
        table { width: 100%; border-collapse: collapse; font-size: 0.85rem; }
        thead th {
            background: linear-gradient(135deg, var(--blue-deep), var(--blue-primary));
            color: var(--white);
            padding: 14px 16px;
            text-align: left;
            font-weight: 700;
            font-size: 0.78rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            white-space: nowrap;
        }
        tbody td {
            padding: 12px 16px;
            border-bottom: 1px solid var(--gray-100);
            color: var(--gray-800);
        }
        tbody tr:nth-child(even) { background: var(--gray-50); }
        tbody tr:hover { background: var(--blue-pale); }
        tbody tr td:first-child { font-weight: 700; color: var(--gray-600); text-align: center; width: 50px; }
        .pagination {
            display: flex; align-items: center; justify-content: center;
            gap: 6px; padding: 16px; flex-wrap: wrap;
        }
        .pagination button {
            padding: 8px 14px; border: 2px solid var(--gray-200);
            border-radius: 8px; background: var(--white);
            font-size: 0.82rem; font-weight: 600; cursor: pointer;
            font-family: inherit; transition: all 0.2s; color: var(--gray-600);
        }
        .pagination button:hover { border-color: var(--blue-light); color: var(--blue-primary); }
        .pagination button.active { background: var(--blue-primary); color: var(--white); border-color: var(--blue-primary); }
        .pagination button:disabled { opacity: 0.4; cursor: not-allowed; }
        .pagination-info { font-size: 0.78rem; color: var(--gray-600); margin: 0 8px; font-weight: 500; }

        /* ====== SEARCH RESULTS ====== */
        .search-results {
            display: none;
            background: var(--white);
            border-radius: 16px;
            box-shadow: 0 4px 16px rgba(0,0,0,0.08);
            padding: 20px;
            margin-bottom: 16px;
            border: 2px solid var(--blue-pale);
        }
        .search-results.show { display: block; }
        .search-results h4 { font-size: 0.85rem; font-weight: 700; color: var(--blue-deep); margin-bottom: 12px; }
        .result-card {
            background: var(--blue-bg);
            border-radius: 12px;
            padding: 14px 18px;
            margin-bottom: 8px;
            border-left: 4px solid var(--blue-light);
        }
        .result-card:last-child { margin-bottom: 0; }
        .result-card .rc-row { display: flex; gap: 8px; margin-bottom: 3px; font-size: 0.84rem; }
        .result-card .rc-label { font-weight: 700; color: var(--blue-primary); min-width: 80px; }
        .result-card .rc-value { color: var(--gray-800); word-break: break-all; }
        .no-results { text-align: center; padding: 20px; color: var(--gray-600); font-size: 0.88rem; }

        /* ====== SETTINGS ====== */
        .settings-grid { display: grid; gap: 20px; }
        .settings-card {
            background: var(--white);
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.06);
        }
        .settings-card h3 {
            font-size: 1rem; font-weight: 700; color: var(--blue-deep);
            margin-bottom: 16px; display: flex; align-items: center; gap: 10px;
            padding-bottom: 12px; border-bottom: 2px solid var(--blue-pale);
        }
        .settings-card h3 i { color: var(--blue-light); }
        .logo-display {
            text-align: center; padding: 20px;
            background: var(--gray-50); border-radius: 12px;
            margin-bottom: 16px;
        }
        .logo-display img { max-width: 180px; max-height: 180px; border-radius: 12px; object-fit: contain; }
        .settings-card textarea {
            width: 100%; padding: 14px;
            border: 2px solid var(--gray-200); border-radius: 12px;
            font-size: 0.9rem; font-family: inherit; resize: vertical;
            min-height: 100px; line-height: 1.6;
            transition: border-color 0.2s;
        }
        .settings-card textarea:focus { outline: none; border-color: var(--blue-light); box-shadow: 0 0 0 3px rgba(25,118,210,0.1); }
        .btn-group { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 14px; }

        /* ====== TOAST ====== */
        .toast-container { position: fixed; top: 20px; right: 20px; z-index: 9999; display: flex; flex-direction: column; gap: 8px; }
        .toast {
            padding: 14px 20px; border-radius: 12px;
            color: var(--white); font-size: 0.85rem; font-weight: 600;
            box-shadow: 0 8px 24px rgba(0,0,0,0.2);
            animation: toastIn 0.3s ease;
            display: flex; align-items: center; gap: 10px;
            max-width: 340px;
        }
        .toast.success { background: var(--green); }
        .toast.error { background: var(--red); }
        .toast.info { background: var(--blue-primary); }
        @keyframes toastIn { from { opacity: 0; transform: translateX(40px); } to { opacity: 1; transform: translateX(0); } }
        @keyframes toastOut { from { opacity: 1; transform: translateX(0); } to { opacity: 0; transform: translateX(40px); } }

        /* ====== OVERLAY ====== */
        .sidebar-overlay {
            display: none; position: fixed; inset: 0;
            background: rgba(0,0,0,0.4); z-index: 90;
        }
        .sidebar-overlay.show { display: block; }

        /* ====== FILE INPUT HIDDEN ====== */
        .hidden-input { display: none; }

        /* ====== RESPONSIVE ====== */
        @media (max-width: 768px) {
            .sidebar { transform: translateX(-100%); }
            .sidebar.open { transform: translateX(0); }
            .main-content { margin-left: 0; }
            .hamburger { display: block; }
            .stat-grid { grid-template-columns: 1fr; }
            .page-content { padding: 16px; }
            .top-bar { padding: 12px 16px; }
            .toolbar { flex-direction: column; align-items: stretch; }
            .search-box { min-width: 100%; }
            .btn-group { flex-direction: column; }
            .btn-group .btn { justify-content: center; }
            .login-card { padding: 28px 24px; }
            table { font-size: 0.78rem; }
            thead th, tbody td { padding: 10px 10px; }
        }
        @media (min-width: 769px) and (max-width: 1024px) {
            .sidebar { width: 220px; }
            .main-content { margin-left: 220px; }
        }

        /* ====== SCROLLBAR ====== */
        ::-webkit-scrollbar { width: 6px; height: 6px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: var(--gray-300); border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--gray-600); }
    </style>
</head>
<body>

    <!-- ========== HALAMAN LOGIN ========== -->
    <div class="login-page" id="loginPage">
        <div class="login-card">
            <img src="https://drive.google.com/thumbnail?id=18-fVQ1HqVhduTzsYSxFj6PqJop4ifDru&sz=w200" alt="Logo" class="login-logo" onerror="this.style.display='none'">
            <h1>SMP Karel Sadsuitubun</h1>
            <p class="subtitle">Sistem Manajemen Peserta Didik</p>
            <div class="form-group">
                <label>Username</label>
                <div class="input-wrapper">
                    <i class="fas fa-user"></i>
                    <input type="text" id="loginUser" placeholder="Masukkan username" autocomplete="off">
                </div>
            </div>
            <div class="form-group">
                <label>Password</label>
                <div class="input-wrapper">
                    <i class="fas fa-lock"></i>
                    <input type="password" id="loginPass" placeholder="Masukkan password">
                </div>
            </div>
            <button class="btn-login" onclick="doLogin()">
                <i class="fas fa-sign-in-alt"></i> Masuk
            </button>
            <div class="login-error" id="loginError"></div>
        </div>
    </div>

    <!-- ========== APLIKASI UTAMA ========== -->
    <div class="app-container" id="appContainer">
        <!-- Overlay sidebar di mobile -->
        <div class="sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>

        <!-- Sidebar -->
        <aside class="sidebar" id="sidebar">
            <div class="sidebar-header">
                <img src="https://drive.google.com/thumbnail?id=18-fVQ1HqVhduTzsYSxFj6PqJop4ifDru&sz=w200" alt="Logo" class="sidebar-logo" id="sidebarLogo" onerror="this.style.display='none'">
                <h2>SMP Karel Sadsuitubun</h2>
            </div>
            <nav class="sidebar-nav">
                <a class="nav-item active" data-page="dashboard" onclick="navigateTo('dashboard')">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a>
                <a class="nav-item" data-page="userpassword" onclick="navigateTo('userpassword')">
                    <i class="fas fa-key"></i> Username & Password
                </a>
                <a class="nav-item" data-page="settings" id="navSettings" onclick="navigateTo('settings')">
                    <i class="fas fa-cog"></i> Pengaturan
                </a>
            </nav>
            <div class="sidebar-footer">
                <a class="nav-item nav-logout" onclick="doLogout()">
                    <i class="fas fa-sign-out-alt"></i> Keluar
                </a>
            </div>
        </aside>

        <!-- Konten Utama -->
        <div class="main-content">
            <header class="top-bar">
                <div class="top-bar-left">
                    <button class="hamburger" id="hamburgerBtn" onclick="toggleSidebar()">
                        <i class="fas fa-bars"></i>
                    </button>
                    <h1 id="pageTitle">Dashboard</h1>
                </div>
                <span class="user-badge" id="userBadge">Administrator</span>
            </header>

            <div class="page-content">
                <!-- ====== DASHBOARD ====== -->
                <section class="page-section active" id="pageDashboard">
                    <div class="stat-grid">
                        <div class="stat-card">
                            <div class="stat-info">
                                <h3>Total Peserta Didik</h3>
                                <div class="stat-number" id="totalPD">0</div>
                            </div>
                            <img src="https://drive.google.com/thumbnail?id=18-fVQ1HqVhduTzsYSxFj6PqJop4ifDru&sz=w200" alt="Logo" class="stat-logo" id="dashLogo" onerror="this.style.display='none'">
                        </div>
                        <div class="stat-card" style="border-left-color: var(--blue-soft);">
                            <div class="stat-info">
                                <h3>Jumlah Username & Password</h3>
                                <div class="stat-number" id="totalUP">0</div>
                            </div>
                            <div style="width:64px;height:64px;border-radius:12px;background:var(--blue-pale);display:flex;align-items:center;justify-content:center;">
                                <i class="fas fa-users" style="font-size:1.6rem;color:var(--blue-primary);"></i>
                            </div>
                        </div>
                    </div>
                    <div class="announcement-card" id="announcementDisplay">
                        <h3><i class="fas fa-bullhorn"></i> Pengumuman</h3>
                        <p id="announcementText">Belum ada pengumuman.</p>
                    </div>
                </section>

                <!-- ====== USERNAME & PASSWORD ====== -->
                <section class="page-section" id="pageUserpassword">
                    <div class="toolbar" id="upToolbar">
                        <button class="btn btn-blue" onclick="downloadPDData()"><i class="fas fa-download"></i> Download</button>
                        <button class="btn btn-green" onclick="document.getElementById('importPDFile').click()"><i class="fas fa-file-import"></i> Import</button>
                        <button class="btn btn-amber" onclick="document.getElementById('uploadPDFile').click()"><i class="fas fa-upload"></i> Upload</button>
                        <input type="file" id="importPDFile" class="hidden-input" accept=".json" onchange="importPDData(event, false)">
                        <input type="file" id="uploadPDFile" class="hidden-input" accept=".json" onchange="importPDData(event, true)">
                    </div>
                    <div class="search-box">
                        <i class="fas fa-search"></i>
                        <input type="text" id="searchInput" placeholder="Cari berdasarkan nama..." oninput="doSearch()">
                    </div>
                    <div class="search-results" id="searchResults">
                        <h4>Hasil Pencarian</h4>
                        <div id="searchResultsList"></div>
                    </div>
                    <div class="table-wrapper">
                        <div class="table-scroll">
                            <table>
                                <thead>
                                    <tr>
                                        <th>No</th>
                                        <th>Username</th>
                                        <th>Nama</th>
                                        <th>Password</th>
                                    </tr>
                                </thead>
                                <tbody id="pdTableBody"></tbody>
                            </table>
                        </div>
                        <div class="pagination" id="pagination"></div>
                    </div>
                </section>

                <!-- ====== PENGATURAN ====== -->
                <section class="page-section" id="pageSettings">
                    <div class="settings-grid">
                        <div class="settings-card">
                            <h3><i class="fas fa-image"></i> Logo Aplikasi</h3>
                            <div class="logo-display">
                                <img src="https://drive.google.com/thumbnail?id=18-fVQ1HqVhduTzsYSxFj6PqJop4ifDru&sz=w400" alt="Logo Sekolah" id="settingsLogoImg" onerror="this.style.display='none'">
                            </div>
                            <p style="font-size:0.82rem;color:var(--gray-600);text-align:center;">Logo resmi SMP Karel Sadsuitubun</p>
                        </div>
                        <div class="settings-card">
                            <h3><i class="fas fa-database"></i> Kelola Data</h3>
                            <p style="font-size:0.85rem;color:var(--gray-600);margin-bottom:14px;">Download keseluruhan data aplikasi atau import data yang pernah dibuat.</p>
                            <div class="btn-group">
                                <button class="btn btn-blue" onclick="downloadAllData()"><i class="fas fa-download"></i> Download Semua Data</button>
                                <button class="btn btn-green" onclick="document.getElementById('importAllFile').click()"><i class="fas fa-file-import"></i> Import Semua Data</button>
                                <input type="file" id="importAllFile" class="hidden-input" accept=".json" onchange="importAllData(event)">
                            </div>
                        </div>
                        <div class="settings-card">
                            <h3><i class="fas fa-bullhorn"></i> Pengumuman</h3>
                            <textarea id="announcementInput" placeholder="Tulis pengumuman di sini..."></textarea>
                            <div class="btn-group">
                                <button class="btn btn-blue" onclick="saveAnnouncement()"><i class="fas fa-save"></i> Simpan Pengumuman</button>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
        </div>
    </div>

    <!-- Toast Container -->
    <div class="toast-container" id="toastContainer"></div>

    <script>
        /* ====================================================
           DATA PESERTA DIDIK (DEFAULT)
        ==================================================== */
        const DEFAULT_PD_DATA = [
            {no:1,username:"KS7A0020001",nama:"Agustina Martha Jalnuhubun",password:"7A020001"},
            {no:2,username:"KS7A0020002",nama:"Alexa Sherryl Jabar",password:"7A020002"},
            {no:3,username:"KS7A0020003",nama:"Billyand Noah Putra Gaga",password:"7A020003"},
            {no:4,username:"KS7A0020004",nama:"Apolinaris Kelanit",password:"7A020004"},
            {no:5,username:"KS7A0020005",nama:"Aprilia Ela Sirwutubun",password:"7A020005"},
            {no:6,username:"KS7A0020006",nama:"Arlinda Metubun",password:"7A020006"},
            {no:7,username:"KS7A0020007",nama:"Bois Alfa Ngutra",password:"7A020007"},
            {no:8,username:"KS7A0020008",nama:"Dewi Putri Madubun",password:"7A020008"},
            {no:9,username:"KS7A0020009",nama:"Elagren Marlatu Lukas Madubun",password:"7A020009"},
            {no:10,username:"KS7A0020010",nama:"Fillano Pedrosa Masbaitubun",password:"7A020010"},
            {no:11,username:"KS7A0020011",nama:"Gracia Viona Yanyaan",password:"7A020011"},
            {no:12,username:"KS7A0020012",nama:"Johanes Obet Bertkat",password:"7A020012"},
            {no:13,username:"KS7A0020013",nama:"Jonathan Alvaro Moriolkosu",password:"7A020013"},
            {no:14,username:"KS7A0020014",nama:"Katarina Rumangun",password:"7A020014"},
            {no:15,username:"KS7A0020015",nama:"Maria Ayu Paisa",password:"7A020015"},
            {no:16,username:"KS7A0020016",nama:"Maria Syelena Suripatty Esomar",password:"7A020016"},
            {no:17,username:"KS7A0020017",nama:"Maria Theresia Rumangun",password:"7A020017"},
            {no:18,username:"KS7A0020018",nama:"Marlina Ingke Balriyanan",password:"7A020018"},
            {no:19,username:"KS7A0020019",nama:"Menci Elsina Fantri Karatem",password:"7A020019"},
            {no:20,username:"KS7A0020020",nama:"Oktovianus Jamrevav",password:"7A020020"},
            {no:21,username:"KS7A0020021",nama:"Reza Dwi Eirlangga Jamlean",password:"7A020021"},
            {no:22,username:"KS7A0020022",nama:"Steven Arnold Laumualy",password:"7A020022"},
            {no:23,username:"KS7A0020023",nama:"Yohana T Resubun",password:"7A020023"},
            {no:24,username:"KS7B0020001",nama:"Agnes Adriana Tawain",password:"7B020001"},
            {no:25,username:"KS7B0020002",nama:"Allmendo Xavi Remetwa",password:"7B020002"},
            {no:26,username:"KS7B0020003",nama:"Alexander Simon Vatubun",password:"7B020003"},
            {no:27,username:"KS7B0020004",nama:"Aroby Tamher",password:"7B020004"},
            {no:28,username:"KS7B0020005",nama:"Aprilia Lidwina Welikin",password:"7B020005"},
            {no:29,username:"KS7B0020006",nama:"Blasius Paskal Dewaut",password:"7B020006"},
            {no:30,username:"KS7B0020007",nama:"Chaylen Manis Koedoeboen",password:"7B020007"},
            {no:31,username:"KS7B0020008",nama:"Firman Tanarubun",password:"7B020008"},
            {no:32,username:"KS7B0020009",nama:"Herlina Abarua",password:"7B020009"},
            {no:33,username:"KS7B0020010",nama:"Julius Marzelino Fadirubun",password:"7B020010"},
            {no:34,username:"KS7B0020011",nama:"Jumadi Tamher",password:"7B020011"},
            {no:35,username:"KS7B0020012",nama:"Kanisius Kebubun",password:"7B020012"},
            {no:36,username:"KS7B0020013",nama:"Mintje Glorya Balubun",password:"7B020013"},
            {no:37,username:"KS7B0020014",nama:"Margaretha Futubun",password:"7B020014"},
            {no:38,username:"KS7B0020015",nama:"Mikha Desire Rukait",password:"7B020015"},
            {no:39,username:"KS7B0020016",nama:"Mawar Sharon Kirwelakubun",password:"7B020016"},
            {no:40,username:"KS7B0020017",nama:"Nahla Abidin Rahakratat",password:"7B020017"},
            {no:41,username:"KS7B0020018",nama:"Penina Rumangun",password:"7B020018"},
            {no:42,username:"KS7B0020019",nama:"Rafail Rengur",password:"7B020019"},
            {no:43,username:"KS7B0020020",nama:"Udeta Insoraki Rumyaan",password:"7B020020"},
            {no:44,username:"KS7B0020021",nama:"Yosep L Takerubun",password:"7B020021"},
            {no:45,username:"KS7B0020022",nama:"Maria A. Heatubun",password:"7B020022"},
            {no:46,username:"KS8A0020001",nama:"Alinda Ode",password:"8A020001"},
            {no:47,username:"KS8A0020002",nama:"Andreas Balubun",password:"8A020002"},
            {no:48,username:"KS8A0020003",nama:"Benedicta Petra Akbar",password:"8A020003"},
            {no:49,username:"KS8A0020004",nama:"Carolus B. Varis Wemaf",password:"8A020004"},
            {no:50,username:"KS8A0020005",nama:"Everets Jiffy Gattu",password:"8A020005"},
            {no:51,username:"KS8A0020006",nama:"Filipho Aditya Somnaikubun",password:"8A020006"},
            {no:52,username:"KS8A0020007",nama:"Gabriel Alferdo Ubyaan",password:"8A020007"},
            {no:53,username:"KS8A0020008",nama:"Giofani Falentino Lutrun",password:"8A020008"},
            {no:54,username:"KS8A0020009",nama:"Yoseph Erlangga Lamere",password:"8A020009"},
            {no:55,username:"KS8A0020010",nama:"Klementina Fernatyanan",password:"8A020010"},
            {no:56,username:"KS8A0020011",nama:"Mediana Aziera Jeujanan",password:"8A020011"},
            {no:57,username:"KS8A0020012",nama:"Rahmatia Bugis",password:"8A020012"},
            {no:58,username:"KS8A0020013",nama:"Reinal Moriolkosu",password:"8A020013"},
            {no:59,username:"KS8A0020014",nama:"Shalmon Antony Rahangmetan",password:"8A020014"},
            {no:60,username:"KS8A0020015",nama:"Vinsensius Edoardus Balubun",password:"8A020015"},
            {no:61,username:"KS8A0020016",nama:"Welhelmus Zyerzeven Ngarbingan",password:"8A020016"},
            {no:62,username:"KS8A0020017",nama:"Marius Rahalus",password:"8A020017"},
            {no:63,username:"KS8A0020018",nama:"Harita F. Laritmas",password:"8A020018"},
            {no:64,username:"KS8A0020019",nama:"Gracia F. Meterai",password:"8A020019"},
            {no:65,username:"KS8B0020001",nama:"adriana theresia rahabav",password:"8B020001"},
            {no:66,username:"KS8B0020002",nama:"Christ Noel Desandro Emyu Souhoka",password:"8B020002"},
            {no:67,username:"KS8B0020003",nama:"Dewanti Sari Jamlean",password:"8B020003"},
            {no:68,username:"KS8B0020004",nama:"Fabiano B. S. Janjaan",password:"8B020004"},
            {no:69,username:"KS8B0020005",nama:"Gabriella Ernesta K. Kanarubun",password:"8B020005"},
            {no:70,username:"KS8B0020006",nama:"Gregorius Higuen Balubun",password:"8B020006"},
            {no:71,username:"KS8B0020007",nama:"Johanis Charles Woersok",password:"8B020007"},
            {no:72,username:"KS8B0020008",nama:"Maria Gratia Maturbongs",password:"8B020008"},
            {no:73,username:"KS8B0020009",nama:"Michael Joseph Masbaitubun",password:"8B020009"},
            {no:74,username:"KS8B0020010",nama:"Oksen Petrus Letsoin",password:"8B020010"},
            {no:75,username:"KS8B0020011",nama:"Paulina Chelsie Renwarin",password:"8B020011"},
            {no:76,username:"KS8B0020012",nama:"Restha Jenifer Rahanra",password:"8B020012"},
            {no:77,username:"KS8B0020013",nama:"Wensislaus Eko Wemaf",password:"8B020013"},
            {no:78,username:"KS8B0020014",nama:"FILIO EMANUEL FAR-FAR",password:"8B020014"},
            {no:79,username:"KS8B0020015",nama:"SYANE SELFI RAHANGMETAN",password:"8B020015"},
            {no:80,username:"KS8B0020016",nama:"MARIA STEFANI ATBAR",password:"8B020016"},
            {no:81,username:"KS8B0020017",nama:"Ronal Regen Rahayaan",password:"8B020017"},
            {no:82,username:"KS8B0020018",nama:"Marwan R. Rahayaan",password:"8B020018"},
            {no:83,username:"KS9A0020001",nama:"ADRIANUS RAHANAU",password:"9A020001"},
            {no:84,username:"KS9A0020002",nama:"ADRIEL CALVINO RENWARIN",password:"9A020002"},
            {no:85,username:"KS9A0020003",nama:"AMOS RISAL OILIRA",password:"9A020003"},
            {no:86,username:"KS9A0020004",nama:"DIANA HERMINA HUAWAE",password:"9A020004"},
            {no:87,username:"KS9A0020005",nama:"VIRGYLIUS M.Y. LEFTUNGUN",password:"9A020005"},
            {no:88,username:"KS9A0020006",nama:"GIDION LEFTA",password:"9A020006"},
            {no:89,username:"KS9A0020007",nama:"INTAN RAHAKBAUW",password:"9A020007"},
            {no:90,username:"KS9A0020008",nama:"KEANO MACKENZIE RAHAJAAN",password:"9A020008"},
            {no:91,username:"KS9A0020009",nama:"LEONTINA JEUJANAN",password:"9A020009"},
            {no:92,username:"KS9A0020010",nama:"MARCELINO ALDO RENYAAN",password:"9A020010"},
            {no:93,username:"KS9A0020011",nama:"MARIA MUTIARA WARAT",password:"9A020011"},
            {no:94,username:"KS9A0020012",nama:"MELATI AUREL ROSYADI",password:"9A020012"},
            {no:95,username:"KS9A0020013",nama:"MICHAELA P. D. TAPOTUBUN",password:"9A020013"},
            {no:96,username:"KS9A0020014",nama:"NILAM MARSDA WABULA",password:"9A020014"},
            {no:97,username:"KS9A0020015",nama:"ROBBEN JAKOBUS OHOILEDJAAN",password:"9A020015"},
            {no:98,username:"KS9A0020016",nama:"ROSNATA PUSPITA RENWARIN",password:"9A020016"},
            {no:99,username:"KS9A0020017",nama:"WELHELMUS RUMANGUN",password:"9A020017"},
            {no:100,username:"KS9A0020018",nama:"WIDY CLEANDRA AZAZYA JABAR",password:"9A020018"},
            {no:101,username:"KS9A0020019",nama:"WILIN NOTANUBUN",password:"9A020019"},
            {no:102,username:"KS9A0020020",nama:"LANA RIECEL LABOBAR",password:"9A020020"},
            {no:103,username:"KS9B0020001",nama:"ANDREAS LORENZO TAKERUBUN",password:"9B020001"},
            {no:104,username:"KS9B0020002",nama:"ALFONSINA OHOIULUN",password:"9B020002"},
            {no:105,username:"KS9B0020003",nama:"AZAN AKBAR TUKLOY",password:"9B020003"},
            {no:106,username:"KS9B0020004",nama:"BERNADETA SIAHAYA",password:"9B020004"},
            {no:107,username:"KS9B0020005",nama:"DINDA PELANGI RENHORAN",password:"9B020005"},
            {no:108,username:"KS9B0020006",nama:"INTAN MASAHIDA",password:"9B020006"},
            {no:109,username:"KS9B0020007",nama:"ISRAEL ABRISYE LABETUBUN",password:"9B020007"},
            {no:110,username:"KS9B0020008",nama:"KRISTINA ANGRENI WELERUBUN",password:"9B020008"},
            {no:111,username:"KS9B0020009",nama:"MARIANUS NGARBINGAN",password:"9B020009"},
            {no:112,username:"KS9B0020010",nama:"MARTHA MELSASAIL",password:"9B020010"},
            {no:113,username:"KS9B0020011",nama:"MUSA EFRUAN",password:"9B020011"},
            {no:114,username:"KS9B0020012",nama:"MARIA BIN METURAN",password:"9B020012"},
            {no:115,username:"KS9B0020013",nama:"NADYA HOEKOEBOEN",password:"9B020013"},
            {no:116,username:"KS9B0020014",nama:"RONI ANGGARA RAWULUNUBUN",password:"9B020014"},
            {no:117,username:"KS9B0020015",nama:"RAHEL M TARANTEIN",password:"9B020015"},
            {no:118,username:"KS9B0020016",nama:"SANDRA JEKLIN FARYO",password:"9B020016"},
            {no:119,username:"KS9B0020017",nama:"YOHANIS MESI EFRUAN",password:"9B020017"},
            {no:120,username:"KS9B0020018",nama:"YOHANIS MARVEL JAMREVAV",password:"9B020018"},
            {no:121,username:"KS9B0020019",nama:"WILHELMUS RAHALOB",password:"9B020019"},
            {no:122,username:"KS9B0020020",nama:"ZULKIFLI RAHAKBAUW",password:"9B020020"}
        ];

        /* ====================================================
           KONFIGURASI LOGIN
        ==================================================== */
        const USERS = [
            { username: "admin", password: "admin", role: "admin", label: "Administrator" },
            { username: "ptsks", password: "ptsks", role: "umum", label: "Pengguna Umum" }
        ];

        /* ====================================================
           STATE APLIKASI
        ==================================================== */
        let currentRole = null;
        let currentPage = "dashboard";
        let currentPageNum = 1;
        const PER_PAGE = 20;

        /* ====================================================
           INISIALISASI
        ==================================================== */
        function init() {
            // Muat data dari localStorage atau gunakan default
            if (!localStorage.getItem("pdData")) {
                localStorage.setItem("pdData", JSON.stringify(DEFAULT_PD_DATA));
            }
            // Set pengumuman default jika belum ada
            if (!localStorage.getItem("announcement")) {
                const defaultAnnouncement = "Jangan lupa subscribe YouTube Channel SMP Karel Sadsuitubun. Ujian OSN akan disiarkan secara live melalui YouTube. Saksikan dan dukung bersama. Jadwal Ujian OSN akan disampaikan kemudian. Terima kasih.";
                localStorage.setItem("announcement", defaultAnnouncement);
            }
            // Cek apakah sudah login
            const savedRole = sessionStorage.getItem("userRole");
            if (savedRole) {
                currentRole = savedRole;
                showApp();
            }
            // Enter key untuk login
            document.getElementById("loginPass").addEventListener("keydown", function(e) {
                if (e.key === "Enter") doLogin();
            });
            document.getElementById("loginUser").addEventListener("keydown", function(e) {
                if (e.key === "Enter") doLogin();
            });
        }

        /* ====================================================
           FUNGSI LOGIN & LOGOUT
        ==================================================== */
        function doLogin() {
            const user = document.getElementById("loginUser").value.trim();
            const pass = document.getElementById("loginPass").value.trim();
            const errEl = document.getElementById("loginError");

            if (!user || !pass) {
                errEl.textContent = "Username dan password harus diisi.";
                return;
            }
            const found = USERS.find(u => u.username === user && u.password === pass);
            if (!found) {
                errEl.textContent = "Username atau password salah.";
                return;
            }
            errEl.textContent = "";
            currentRole = found.role;
            sessionStorage.setItem("userRole", found.role);
            showApp();
        }

        function doLogout() {
            currentRole = null;
            sessionStorage.removeItem("userRole");
            document.getElementById("loginPage").style.display = "flex";
            document.getElementById("appContainer").classList.remove("active");
            document.getElementById("loginUser").value = "";
            document.getElementById("loginPass").value = "";
            document.getElementById("loginError").textContent = "";
        }

        /* ====================================================
           TAMPILKAN APLIKASI
        ==================================================== */
        function showApp() {
            document.getElementById("loginPage").style.display = "none";
            document.getElementById("appContainer").classList.add("active");

            // Set badge user
            const found = USERS.find(u => u.role === currentRole);
            document.getElementById("userBadge").textContent = found ? found.label : "";

            // Sembunyikan menu Pengaturan untuk role umum
            const navSettings = document.getElementById("navSettings");
            navSettings.style.display = currentRole === "admin" ? "flex" : "none";

            // Sembunyikan tombol aksi di User Password untuk role umum
            const toolbar = document.getElementById("upToolbar");
            toolbar.style.display = currentRole === "admin" ? "flex" : "none";

            // Muat data dashboard
            navigateTo("dashboard");
        }

        /* ====================================================
           NAVIGASI SIDEBAR
        ==================================================== */
        function navigateTo(page) {
            currentPage = page;
            currentPageNum = 1;

            // Update menu aktif
            document.querySelectorAll(".nav-item[data-page]").forEach(el => {
                el.classList.toggle("active", el.dataset.page === page);
            });

            // Update halaman aktif
            document.querySelectorAll(".page-section").forEach(el => el.classList.remove("active"));

            const titles = { dashboard: "Dashboard", userpassword: "Username & Password", settings: "Pengaturan" };
            document.getElementById("pageTitle").textContent = titles[page] || "";

            if (page === "dashboard") {
                document.getElementById("pageDashboard").classList.add("active");
                renderDashboard();
            } else if (page === "userpassword") {
                document.getElementById("pageUserpassword").classList.add("active");
                renderPDTable();
                // Reset pencarian
                document.getElementById("searchInput").value = "";
                document.getElementById("searchResults").classList.remove("show");
            } else if (page === "settings") {
                document.getElementById("pageSettings").classList.add("active");
                renderSettings();
            }

            // Tutup sidebar di mobile
            closeSidebar();
        }

        /* ====================================================
           SIDEBAR MOBILE
        ==================================================== */
        function toggleSidebar() {
            document.getElementById("sidebar").classList.toggle("open");
            document.getElementById("sidebarOverlay").classList.toggle("show");
        }
        function closeSidebar() {
            document.getElementById("sidebar").classList.remove("open");
            document.getElementById("sidebarOverlay").classList.remove("show");
        }

        /* ====================================================
           DASHBOARD
        ==================================================== */
        function renderDashboard() {
            const data = getPDData();
            document.getElementById("totalPD").textContent = data.length;
            document.getElementById("totalUP").textContent = data.length;

            // Tampilkan pengumuman
            const announcement = localStorage.getItem("announcement") || "Belum ada pengumuman.";
            document.getElementById("announcementText").textContent = announcement;
        }

        /* ====================================================
           TABEL USERNAME & PASSWORD
        ==================================================== */
        function getPDData() {
            try {
                return JSON.parse(localStorage.getItem("pdData")) || [];
            } catch (e) {
                return [];
            }
        }

        function savePDData(data) {
            localStorage.setItem("pdData", JSON.stringify(data));
        }

        function renderPDTable() {
            const data = getPDData();
            const totalPages = Math.max(1, Math.ceil(data.length / PER_PAGE));
            if (currentPageNum > totalPages) currentPageNum = totalPages;

            const start = (currentPageNum - 1) * PER_PAGE;
            const end = start + PER_PAGE;
            const pageData = data.slice(start, end);

            const tbody = document.getElementById("pdTableBody");
            if (pageData.length === 0) {
                tbody.innerHTML = '<tr><td colspan="4" style="text-align:center;padding:30px;color:var(--gray-600);">Tidak ada data.</td></tr>';
            } else {
                let html = "";
                pageData.forEach((item, idx) => {
                    const no = start + idx + 1;
                    html += `<tr>
                        <td>${no}</td>
                        <td style="font-weight:600;color:var(--blue-primary);">${escHtml(item.username)}</td>
                        <td>${escHtml(item.nama)}</td>
                        <td style="font-family:monospace;font-weight:600;letter-spacing:0.5px;">${escHtml(item.password)}</td>
                    </tr>`;
                });
                tbody.innerHTML = html;
            }

            // Render pagination
            renderPagination(data.length, totalPages);
        }

        function renderPagination(total, totalPages) {
            const container = document.getElementById("pagination");
            if (totalPages <= 1) { container.innerHTML = ""; return; }

            const start = (currentPageNum - 1) * PER_PAGE + 1;
            const end = Math.min(currentPageNum * PER_PAGE, total);

            let html = `<button ${currentPageNum === 1 ? 'disabled' : ''} onclick="goToPage(${currentPageNum - 1})"><i class="fas fa-chevron-left"></i></button>`;

            // Tampilkan nomor halaman dengan batasan
            let pages = [];
            if (totalPages <= 7) {
                for (let i = 1; i <= totalPages; i++) pages.push(i);
            } else {
                pages.push(1);
                if (currentPageNum > 3) pages.push("...");
                for (let i = Math.max(2, currentPageNum - 1); i <= Math.min(totalPages - 1, currentPageNum + 1); i++) {
                    pages.push(i);
                }
                if (currentPageNum < totalPages - 2) pages.push("...");
                pages.push(totalPages);
            }

            pages.forEach(p => {
                if (p === "...") {
                    html += `<span class="pagination-info">...</span>`;
                } else {
                    html += `<button class="${p === currentPageNum ? 'active' : ''}" onclick="goToPage(${p})">${p}</button>`;
                }
            });

            html += `<span class="pagination-info">${start}-${end} dari ${total}</span>`;
            html += `<button ${currentPageNum === totalPages ? 'disabled' : ''} onclick="goToPage(${currentPageNum + 1})"><i class="fas fa-chevron-right"></i></button>`;

            container.innerHTML = html;
        }

        function goToPage(page) {
            currentPageNum = page;
            renderPDTable();
            // Scroll ke atas tabel
            document.querySelector(".table-wrapper").scrollIntoView({ behavior: "smooth", block: "start" });
        }

        /* ====================================================
           PENCARIAN USERNAME & PASSWORD
        ==================================================== */
        function doSearch() {
            const query = document.getElementById("searchInput").value.trim().toLowerCase();
            const resultsDiv = document.getElementById("searchResults");
            const listDiv = document.getElementById("searchResultsList");

            if (query.length < 1) {
                resultsDiv.classList.remove("show");
                return;
            }

            const data = getPDData();
            const filtered = data.filter(d => d.nama.toLowerCase().includes(query));

            if (filtered.length === 0) {
                listDiv.innerHTML = '<div class="no-results"><i class="fas fa-search" style="margin-right:6px;"></i>Tidak ditemukan data untuk "' + escHtml(query) + '"</div>';
            } else {
                let html = "";
                filtered.forEach(d => {
                    html += `<div class="result-card">
                        <div class="rc-row"><span class="rc-label">Username:</span><span class="rc-value">${escHtml(d.username)}</span></div>
                        <div class="rc-row"><span class="rc-label">Nama:</span><span class="rc-value">${escHtml(d.nama)}</span></div>
                        <div class="rc-row"><span class="rc-label">Password:</span><span class="rc-value">${escHtml(d.password)}</span></div>
                    </div>`;
                });
                listDiv.innerHTML = html;
            }
            resultsDiv.classList.add("show");
        }

        /* ====================================================
           DOWNLOAD DATA PD (JSON)
        ==================================================== */
        function downloadPDData() {
            const data = getPDData();
            const blob = new Blob([JSON.stringify(data, null, 2)], { type: "application/json" });
            downloadBlob(blob, "data_username_password.json");
            showToast("Data berhasil diunduh.", "success");
        }

        /* ====================================================
           IMPORT & UPLOAD DATA PD
        ==================================================== */
        function importPDData(event, isAppend) {
            const file = event.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const imported = JSON.parse(e.target.result);
                    if (!Array.isArray(imported)) throw new Error("Format tidak valid");

                    // Validasi struktur data
                    const valid = imported.every(d => d.username && d.nama && d.password);
                    if (!valid) throw new Error("Struktur data tidak sesuai. Pastikan setiap item memiliki username, nama, dan password.");

                    if (isAppend) {
                        // Upload = tambahkan ke data yang sudah ada
                        const existing = getPDData();
                        let startNo = existing.length;
                        const newItems = imported.map((d, i) => ({
                            no: startNo + i + 1,
                            username: d.username,
                            nama: d.nama,
                            password: d.password
                        }));
                        const merged = existing.concat(newItems);
                        savePDData(merged);
                        showToast(`${newItems.length} data berhasil ditambahkan.`, "success");
                    } else {
                        // Import = ganti semua data
                        const numbered = imported.map((d, i) => ({
                            no: i + 1,
                            username: d.username,
                            nama: d.nama,
                            password: d.password
                        }));
                        savePDData(numbered);
                        showToast(`${numbered.length} data berhasil diimpor.`, "success");
                    }
                    renderPDTable();
                } catch (err) {
                    showToast("Gagal import: " + err.message, "error");
                }
            };
            reader.readAsText(file);
            event.target.value = "";
        }

        /* ====================================================
           PENGATURAN
        ==================================================== */
        function renderSettings() {
            document.getElementById("announcementInput").value = localStorage.getItem("announcement") || "";
        }

        function saveAnnouncement() {
            const text = document.getElementById("announcementInput").value.trim();
            localStorage.setItem("announcement", text);
            showToast("Pengumuman berhasil disimpan.", "success");
        }

        /* ====================================================
           DOWNLOAD SEMUA DATA (FULL BACKUP)
        ==================================================== */
        function downloadAllData() {
            const allData = {
                pdData: getPDData(),
                announcement: localStorage.getItem("announcement") || ""
            };
            const blob = new Blob([JSON.stringify(allData, null, 2)], { type: "application/json" });
            downloadBlob(blob, "backup_data_lengkap.json");
            showToast("Semua data berhasil diunduh.", "success");
        }

        /* ====================================================
           IMPORT SEMUA DATA (FULL RESTORE)
        ==================================================== */
        function importAllData(event) {
            const file = event.target.files[0];
            if (!file) return;

            const reader = new FileReader();
            reader.onload = function(e) {
                try {
                    const imported = JSON.parse(e.target.result);
                    if (!imported.pdData || !Array.isArray(imported.pdData)) {
                        throw new Error("Format file tidak valid. Pastikan file merupakan backup yang benar.");
                    }
                    // Restore data PD
                    savePDData(imported.pdData);
                    // Restore pengumuman
                    if (imported.announcement !== undefined) {
                        localStorage.setItem("announcement", imported.announcement);
                    }
                    showToast("Semua data berhasil diimpor dan dipulihkan.", "success");
                    renderSettings();
                } catch (err) {
                    showToast("Gagal import: " + err.message, "error");
                }
            };
            reader.readAsText(file);
            event.target.value = "";
        }

        /* ====================================================
           UTILITAS
        ==================================================== */
        function escHtml(str) {
            const div = document.createElement("div");
            div.textContent = str;
            return div.innerHTML;
        }

        function downloadBlob(blob, filename) {
            const url = URL.createObjectURL(blob);
            const a = document.createElement("a");
            a.href = url;
            a.download = filename;
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
        }

        function showToast(message, type) {
            const container = document.getElementById("toastContainer");
            const toast = document.createElement("div");
            toast.className = "toast " + (type || "info");
            const icons = { success: "fa-check-circle", error: "fa-times-circle", info: "fa-info-circle" };
            toast.innerHTML = `<i class="fas ${icons[type] || icons.info}"></i> ${escHtml(message)}`;
            container.appendChild(toast);

            setTimeout(() => {
                toast.style.animation = "toastOut 0.3s ease forwards";
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        /* ====================================================
           MULAI APLIKASI
        ==================================================== */
        init();
    </script>
</body>
</html>
